description: >
  GEOS-ESM docker executor

parameters:
  resource_class:
    description: "Resource class to use"
    default: large
    type: string

machine:
  image: default

resource_class: << parameters.resource_class >>
